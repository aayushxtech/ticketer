version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount the project directory into the container so code changes
      # are reflected immediately during development (no rebuild needed
      # for source-only changes).
      - .:/app
    env_file:
      - .env
